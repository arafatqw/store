# نظام جرد المخازن - Storeee

## ✅ تم التحويل إلى Laravel بنجاح!

النظام الآن يعمل على:
- **Backend**: Laravel 12 (PHP)
- **Frontend**: React + Vite
- **Database**: SQLite (جاهز للاستخدام)

---

## 🚀 التشغيل السريع

### الطريقة الأسهل - ملف واحد:
- **Windows**: اضغط مرتين على `start-all.bat`
- **PowerShell**: شغل `.\start-all.ps1`

سيتم تشغيل Laravel و React تلقائياً في نوافذ منفصلة!

---

## 📝 التشغيل اليدوي

### نافذة 1 - Laravel Backend:
```bash
cd backend
php artisan serve
```
يعمل على: **http://localhost:8000**

### نافذة 2 - React Frontend:
```bash
cd client
npm run dev
```
يعمل على: **http://localhost:3000**

---

## 🎯 المميزات

✅ إدارة المنتجات (إضافة، تعديل، حذف، بحث)
✅ إدارة المخازن مع مدير وموقع المدير
✅ نظام الفِرق (الإنترنت، الكهرباء، الماء)
✅ ربط المنتجات بالفِرق
✅ متابعة المخزون في الوقت الفعلي
✅ تسجيل حركات الدخول والخروج
✅ صفحة تفاصيل المخزن مع فلترة حسب الفريق
✅ واجهة مستخدم عصرية ومتجاوبة

---

## 📁 البنية

```
Storeee/
├── backend/          # Laravel Backend
│   ├── app/
│   ├── database/
│   └── routes/
├── client/           # React Frontend
│   └── src/
├── start-all.bat     # تشغيل كل شيء (Windows)
└── start-all.ps1     # تشغيل كل شيء (PowerShell)
```

---

## 🔧 API Endpoints

جميع الـ API تحت `/api/`:

- `GET /api/teams` - قائمة الفِرق
- `POST /api/teams` - إضافة فريق
- `GET /api/warehouses` - قائمة المخازن
- `POST /api/warehouses` - إضافة مخزن
- `GET /api/products` - قائمة المنتجات
- `POST /api/products` - إضافة منتج
- `GET /api/transactions` - قائمة الحركات
- `POST /api/transactions` - إضافة حركة
- `GET /api/inventory?warehouseId=xxx&teamId=xxx` - المخزون

---

## 📊 قاعدة البيانات

قاعدة البيانات: `backend/database/database.sqlite`

تم إنشاء جميع الجداول تلقائياً:
- `teams` - الفِرق
- `warehouses` - المخازن
- `products` - المنتجات
- `transactions` - الحركات

**الفِرق الافتراضية:**
- فريق الإنترنت
- فريق الكهرباء
- فريق الماء

---

## ✅ التحقق من التشغيل

1. افتح المتصفح: http://localhost:8000/api/teams
   - يجب أن ترى JSON بالفِرق

2. افتح: http://localhost:3000
   - يجب أن ترى واجهة النظام

---

## 🎉 النظام جاهز للاستخدام!

**ملاحظة**: تأكد من أن Laravel و React يعملان في نفس الوقت.

