# إعداد HTTPS للمشروع 🔒

## ✅ تم تفعيل HTTPS

تم إعداد HTTPS تلقائياً باستخدام `vite-plugin-mkcert` لتمكين الكاميرا على Chrome على الهاتف.

## 📱 الوصول من الهاتف:

### العنوان الجديد:
```
https://192.168.1.106:3000
```

**ملاحظة:** استخدم **https://** (وليس http://)

## ⚠️ تحذير الشهادة:

عند فتح الموقع لأول مرة من الهاتف، قد ترى تحذير:

```
Your connection is not private
NET::ERR_CERT_AUTHORITY_INVALID
```

### الحل:
1. اضغط على **"Advanced"** أو **"متقدم"**
2. اضغط على **"Proceed to 192.168.1.106 (unsafe)"** أو **"متابعة إلى الموقع"**
3. سيتم حفظ الشهادة ولن يظهر التحذير مرة أخرى

## 🔧 كيف يعمل:

- يستخدم `vite-plugin-mkcert` لإنشاء شهادة SSL محلية تلقائياً
- الشهادة صالحة فقط على جهازك (local development)
- لا تحتاج لأي إعدادات إضافية

## 🚀 التشغيل:

```bash
cd client
npm run dev
```

سيبدأ السيرفر على:
- **Local**: `https://localhost:3000`
- **Network**: `https://192.168.1.106:3000`

## ✅ الفوائد:

1. ✅ الكاميرا تعمل على Chrome على الهاتف
2. ✅ لا حاجة لـ Firefox كحل بديل
3. ✅ يعمل بشكل طبيعي على جميع المتصفحات
4. ✅ آمن للشبكة المحلية

---

**الآن يمكنك استخدام الكاميرا من Chrome على الهاتف! 📸**

