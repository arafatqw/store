# الوصول من الهاتف المحمول 📱

## المتطلبات:
1. ✅ اللابتوب والهاتف على نفس شبكة Wi-Fi
2. ✅ تشغيل السيرفرات على الشبكة المحلية

## IP Address الحالي:
**192.168.1.106**

## طريقة الوصول:

### 1. تشغيل السيرفرات:
```bash
# من المجلد الرئيسي
start-all.bat
# أو
start-all.ps1
```

### 2. فتح النظام من الهاتف:

افتح المتصفح في الهاتف (Chrome, Firefox, etc.) واكتب:

```
https://192.168.1.106:3000
```

⚠️ **ملاحظة مهمة:**
- النظام يعمل الآن على **HTTPS** (وليس HTTP)
- هذا ضروري لكي تعمل الكاميرا على Chrome على الهاتف
- عند أول فتح، قد يظهر تحذير "غير آمن" - اضغط "Advanced" → "Proceed to site" أو "متابعة إلى الموقع"

## ملاحظات مهمة:

### إذا لم يعمل:

1. **تأكد من Firewall:**
   - افتح Windows Firewall
   - اسمح للـ Port 3000 و 8000

2. **تحقق من IP Address:**
   ```powershell
   ipconfig
   ```
   - ابحث عن `IPv4 Address` في قسم `Wireless LAN adapter Wi-Fi`
   - إذا تغير IP، حدث `vite.config.js` و `start-all.*` بالـ IP الجديد

3. **تأكد أن السيرفرات تعمل:**
   - Laravel: يجب أن ترى `Server running on [http://0.0.0.0:8000]`
   - React: يجب أن ترى `Local: http://localhost:3000` و `Network: http://192.168.1.106:3000`

4. **على الهاتف:**
   - استخدم HTTPS إذا طلبت المتصفح
   - أو استخدم `http://` مباشرة (بدون s)

## تغيير IP Address:

إذا تغير IP Address في المستقبل:

1. افتح `client/vite.config.js`
2. غيّر `192.168.1.106` إلى IP الجديد
3. افتح `start-all.bat` و `start-all.ps1`
4. غيّر `192.168.1.106` إلى IP الجديد

## استخدام الكاميرا:

- عند فتح النظام من الهاتف، يمكنك استخدام زر "مسح" لفتح الكاميرا
- ستحتاج لإعطاء الإذن للوصول للكاميرا عند أول استخدام
- تأكد من السماح للوصول للكاميرا في إعدادات المتصفح

---

**الآن يمكنك استخدام النظام من هاتفك!** 🎉

