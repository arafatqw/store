# 🚀 دليل التشغيل السريع

## الطريقة الأسهل:

### Windows:
```
اضغط مرتين على: start-all.bat
```

### PowerShell:
```powershell
.\start-all.ps1
```

---

## أو يدوياً:

### 1. شغّل Laravel:
```bash
cd backend
php artisan serve
```

### 2. شغّل React (في نافذة أخرى):
```bash
cd client
npm run dev
```

### 3. افتح المتصفح:
- Frontend: http://localhost:3000
- Backend API: http://localhost:8000/api/teams

---

## ✅ إذا كان كل شيء يعمل، ستشوف:

✅ Laravel: "Laravel development server started"
✅ React: "Local: http://localhost:3000"
✅ لا توجد أخطاء ECONNREFUSED في Vite

---

**ملاحظة مهمة**: يجب أن يعمل Laravel و React معاً!

